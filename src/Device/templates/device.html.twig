{% extends '@layout/default.html.twig' %}

{% block title %}Device{% endblock %}

{% block content %}
    <div class="row">
        <div class="col">
            <table class="table">
                <thead>
                <tr>
                    <th>Sensor name</th>
                    <th>Value</th>
                </tr>
                </thead>
                <tbody>
                {% for sensor in sensorData %}
                    <tr>
                        <td>{{ sensor.sensor }}</td>
                        <td>{{ sensor.state }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
