{% extends '@layout/default.html.twig' %}

{% block title %}{{ device.name }}{% endblock %}

{% block content %}
    <div class="row">
        <div class="col">
            {{ summary|raw }}
        </div>
    </div>

    <a href="{{ path('devices.sensors', { 'name': device.name }) }}">View all sensor data</a>
{% endblock %}
